function Tag({ tag }) {
  return (
    <span className="ring-primary   items-center rounded-full bg-transparent px-2 py-1 text-[10px] font-semibold  text-purple-700 ring-1 ">
      {tag}
    </span>
  );
}

export default Tag;
